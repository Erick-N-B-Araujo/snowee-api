pipeline{
    agent {
        label 'remoteJenkins'
    }
    environment {
		PASS = credentials('dockerhub-pass')
	}
    stages {
        stage('Deploy') {
            steps {
                sh ''' 
                    ./remote/scripts/deploy/stop_app.sh
                    ./remote/scripts/deploy/deploy_app.sh
                '''
            }
        }
    }
}